#include <stdio.h>
/**
 * main - prints single difits in base 10
 * Return: Always 0
 */
int main(void)
{
        char le;
        for ( le = 'z'; le <= a; le--)
                putchar(le);
        putchar('\n');
        return (0);
}
